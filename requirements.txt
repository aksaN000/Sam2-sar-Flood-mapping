# Pinned dependencies for the modelling stack (training and evaluation).
# These are independent of requirements.txt, which covers the dataset
# construction pipeline. Both files can be installed into the same
# Python 3.11 virtualenv without conflicts.
#
# Install on a CUDA 12.4 host with:
#     pip install -r requirements-train.txt --extra-index-url \
#         https://download.pytorch.org/whl/cu124

# Core deep learning stack
torch>=2.5,<2.6
torchvision>=0.20,<0.21

# HuggingFace ecosystem (4.45 is the first release with native SAM 2 support)
transformers>=4.45
peft>=0.13
accelerate>=1.0

# Training framework and helpers
pytorch-lightning>=2.4
kornia>=0.7
timm>=1.0

# Experiment tracking and config management
wandb>=0.18
omegaconf>=2.3
hydra-core>=1.3

# Metrics and evaluation
torchmetrics>=1.5
scikit-learn>=1.5

# Utility (for reading the chip index CSV and label rasters during eval)
pandas>=2.2
